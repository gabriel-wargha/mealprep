.DS_Store
.env
/node_modules/

# React Router
/.react-router/
/build/

# Local env files
.env.*
!.env.example

# Prisma (Day 8+) — the local SQLite database
/prisma/*.db
/prisma/*.db-journal

# Personal build plan / notes (kept local, not part of the app)
/MealPrep-10-Day-Plan/
